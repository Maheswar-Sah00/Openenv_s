# Copy to .env and fill for local runs (optional; inference uses os.environ).
# Phase 2 / LiteLLM proxy — do not hardcode real keys in the repo.

# API_BASE_URL=https://router.huggingface.co/v1
# API_KEY=
# HF_TOKEN=
# MODEL_NAME=Qwen/Qwen2.5-72B-Instruct

# Scam env / Space
# SCAM_ENV_TASK=easy
# PORT=7860
# BENCHMARK=scam-detection-env
# SCAM_ENV_MAX_RUNTIME_SEC=1140
# SUCCESS_SCORE_THRESHOLD=0.8
